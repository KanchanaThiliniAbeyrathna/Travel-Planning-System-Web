{% extends 'userbase.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-primary">All Visiting Places
                <a class="btn btn-outline btn-info pull-right" href="{{ path('visitingplace_new') }}">Create New</a></h1>
            </div>
            
        </div>
        <div class="row">
            <div class="col-lg-12">
            
                {% for visitingplace in visitingplaces %}
                    <div class="col-lg-4">
                        <div class="panel panel-info">
                            <div class="panel-body">
                                <div class="row">
                                    <div class="col-lg-12">   
                                        <img src="{{asset(visitingplace.path)}}" width="275" height="275">
                                        <p class="lead text-center text-primary" >{{ visitingplace.name }}</p>
                                        
                                        
                                    </div>
                                </div>
                            </div>
                            <a class="btn btn-block btn-info" href="{{ path('visitingplace_show', { 'id': visitingplace.id }) }}">View Details  <i class="fa fa-arrow-circle-right"></i></a>
                        </div>
                    </div>
                {% endfor %}

            </div>
        </div>
    </div>
{% endblock %}
